<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFramework>net6.0</TargetFramework>
        <RootNamespace />
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>
    </PropertyGroup>
    
    <PropertyGroup>
        <DeviceNetVersion>4.2.1</DeviceNetVersion>
    </PropertyGroup>

    <ItemGroup>
      <PackageReference Include="Device.Net" Version="$(DeviceNetVersion)" />
      <PackageReference Include="Hid.Net" Version="$(DeviceNetVersion)" />
    </ItemGroup>

</Project>
